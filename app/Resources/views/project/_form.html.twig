{% form_theme form 'bootstrap_3_layout_with_help.html.twig' %}

{{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_rest(form) }}

    <button type="submit" class="btn btn-default">Enregistrer</button>
{{ form_end(form) }}